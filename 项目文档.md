# 音乐版权保护平台 — 项目文档

> 基于 Hyperledger Fabric 的音乐作品版权存证与授权管理系统

## 一、项目概述

本项目围绕**音乐作品版权存证与授权**场景，实现了一个基于 Hyperledger Fabric 区块链的数字版权保护应用（B/S 架构）。创作者可将音乐作品的 SHA-256 数字指纹登记上链，利用区块链不可篡改、可追溯的特性完成版权确权；版权方可通过链上智能合约向使用方授予/验证/撤销授权，并可发起争议存证、查询作品完整流转历史。

系统采用 **Vue 3 前端 + Go(Gin) 后端 + Hyperledger Fabric 2.5 区块链层**的三层架构，业务数据链上存证、用户数据链下 MySQL 存储，兼顾可信性与性能。

## 二、解决的实际问题与应用场景

### 2.1 现实痛点

| 痛点 | 传统方式的问题 | 本平台的解决方式 |
|---|---|---|
| 确权难 | 版权登记流程繁琐、周期长、成本高 | 登记时自动计算音频文件 SHA-256 指纹，一键上链，即时确权 |
| 取证难 | 侵权纠纷中"谁先创作"难以自证 | 链上存证带时间戳与交易 ID，不可篡改，可随时验证 |
| 授权乱 | 授权靠线下合同，真假难辨、状态不透明 | 授权全生命周期上链（授予→验证→撤销→使用计数），任何一方可在线验证授权真伪 |
| 溯源难 | 版权转让历史散落在各中介手中 | `GetHistory` 读取账本历史，完整呈现作品从登记到每次转让的链路 |

### 2.2 目标用户与典型场景

- **音乐创作者**（唱作人、独立音乐人）：登记作品、下载存证证书、管理名下版权
- **版权方 / 经纪机构**：授权商业化使用、转让版权、监控使用次数
- **使用方**（平台、广告商、视频制作者）：验证授权有效性后再使用，规避侵权风险
- **争议方**：对权属存疑时提交争议证据上链存证

## 三、产品方案与功能描述

### 3.1 功能模块

**① 版权登记与确权（works 模块）**
- 分步向导式登记：上传音频文件（前端 Web Crypto 预计算哈希供预览）→ 填写作品元信息 → 后端复算 SHA-256 指纹上链（文件本体不落盘、不上链）→ 返回交易 ID
- 哈希验证：对任意文件重算哈希与链上比对，判断是否为已登记作品
- 版权转让：将作品所有权转移给其他用户，链上留痕
- 存证证书：生成 PDF 版权证书供下载
- 作品详情 / 我的作品 / 作品搜索

**② 授权管理（licenses 模块）**
- 授予授权：支持商用 / 非商用 / 独家三种类型，可设起止时间与最大使用次数
- 授权验证：输入作品与被授权方，合约自动校验授权状态、有效期、次数限制，返回结构化验证结果
- 我的授权、撤销授权、使用记录上报（UsedCount 计数）

**③ 争议处理（disputes 模块）**
- 发起争议：提交声明与证据描述上链存证（PENDING / RESOLVED 状态）
- 按作品查询全部争议记录

**④ 用户与工作台**
- 注册 / 登录（JWT 鉴权，密码 bcrypt 加盐存储）
- Dashboard 工作台：版权 / 授权 / 争议数据统计与可视化（ECharts），最近授权、最近争议速览

### 3.2 链上数据模型

| 数据对象 | 关键字段 | 说明 |
|---|---|---|
| MusicWork（音乐作品） | WorkID、Title、Artist、OwnerID、FileHash、Status(ACTIVE/TRANSFERRED/DISPUTED)、TxID | 版权主对象 |
| License（授权） | LicenseID、WorkID、GrantorID、LicenseeID、Type、起止时间、MaxUsage/UsedCount、Status | 授权全生命周期 |
| DisputeRecord（争议） | DisputeID、WorkID、ClaimantID、Evidence、Status | 争议存证 |

## 四、技术开发方案

### 4.1 总体架构

```
┌─────────────────────────────────────────────────┐
│  前端  Vue 3 + Element Plus + Pinia + ECharts    │
│  （音乐厅之夜暗色主题，Vite 构建）                 │
└──────────────────────┬──────────────────────────┘
                       │ REST API (JSON) + JWT
┌──────────────────────▼──────────────────────────┐
│  后端  Go + Gin                                  │
│  Handler → Service → FabricClient                │
│  GORM + MySQL（用户表、版权缓存）                  │
│  gofpdf 证书生成 / SHA-256 摘要                   │
└──────────────────────┬──────────────────────────┘
                       │ Fabric Gateway SDK (gRPC + TLS)
┌──────────────────────▼──────────────────────────┐
│  区块链层  Hyperledger Fabric 2.5 (Docker)       │
│  orderer.example.com                             │
│  peer0.org1.example.com + CouchDB                │
│  peer0.org2.example.com + CouchDB                │
│  链码 copyright（Go，双组织安装，mychannel）       │
└─────────────────────────────────────────────────┘
```

### 4.2 技术选型

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Vue 3、Vue Router、Pinia、Element Plus、ECharts、Sass | SPA 单页应用，暗色主题 token 化样式管理 |
| 后端 | Go 1.22、Gin、GORM、MySQL、JWT、gofpdf | 分层架构：api/handler、service、model、utils、middleware |
| 区块链 | Hyperledger Fabric 2.5、fabric-gateway v1.7 SDK | 2 组织 2 Peer + Solo Orderer，CouchDB 富查询 |
| 链码 | Go + fabric-contract-api | 16 个对外合约函数，含复合键索引与 CouchDB 富查询 |
| 部署 | Docker Compose、shell 脚本 | bootstrap / deploy / teardown 一键启停 |

### 4.3 关键技术设计

- **链下链上混合存储**：文件本体与用户账号不上链；链上仅存文件 SHA-256 指纹与版权业务数据，兼顾隐私与性能
- **双端哈希校验**：前端用 Web Crypto (`crypto.subtle.digest`) 本地预计算 SHA-256 供登记前预览，后端对上传文件复算 SHA-256 作为权威指纹上链；仅哈希上链，文件本体不落盘
- **Fabric Gateway 对接**：后端以 Org1 的 User1 身份（X.509 证书 + TLS）通过 fabric-gateway SDK 连接 Peer，对提交/查询分别设置超时
- **二级索引查询**：链码为授权建立复合键索引（`queryLicensesByIndex`），支持按作品、按被授权方高效检索
- **历史溯源**：基于 Fabric `GetHistoryForKey` 实现作品每次状态变更（登记/转让等）的时间线回溯，含交易 ID 与时间戳
- **授权验证逻辑**：合约侧综合校验授权存在性、状态（ACTIVE/REVOKED）、有效期区间、使用次数上限，返回 `valid / reason / license` 结构化结果

### 4.4 链码合约函数清单（16 个对外函数）

| 分类 | 函数 |
|---|---|
| 作品 | RegisterWork、QueryWork、QueryWorksByOwner、SearchWorks、VerifyFileHash、TransferCopyright、GetHistory |
| 授权 | GrantLicense、VerifyLicense、QueryLicense、QueryLicensesByWork、QueryLicensesByLicensee、RevokeLicense、RecordUsage |
| 争议 | FileDispute、QueryDisputesByWork |

### 4.5 后端 REST 接口（/api/v1）

| 模块 | 接口 |
|---|---|
| 用户 | POST /user/register、POST /user/login、GET /user/profile |
| 版权 | POST /copyright/register、GET /copyright/:workID、GET /copyright/my/list、GET /copyright/search、GET /copyright/:workID/history、POST /copyright/verify-hash、POST /copyright/transfer、GET /copyright/:workID/certificate |
| 授权 | POST /license/grant、GET /license/verify、GET /license/my、POST /license/revoke、POST /license/record-usage |
| 争议 | POST /dispute/file、GET /dispute/:workID |

（除注册/登录外均需 JWT 认证）

## 五、最终成果

1. **完整可运行的区块链 B/S 应用**：前端 15 个页面、后端 18 个接口、链码 16 个对外合约函数，覆盖"登记→确权→授权→验证→争议→溯源→证书"版权业务全流程
2. **Fabric 2.5 网络工程化部署方案**：docker-compose 编排（Orderer + 双 Peer + CouchDB + CLI）、crypto-config / configtx 配置、bootstrap / deploy / teardown 脚本
3. **可视化工作台**：数据统计图表与最近授权/争议面板，"音乐厅之夜"暗色主题界面

## 六、团队组成与分工

| 成员 | 主要任务 |
|---|---|
| 谢和泳 | 后端代码 + 项目文档 |
| 狄毓灵 | 前端代码 |
| 张博渊 | PPT |
| 刘欣瓒 | 讲解视频 + pre |

## 七、后期改进思路

1. **存证增强**：接入 IPFS 存储音频文件密文，链上存哈希 + IPFS CID，实现文件可取回；引入音频指纹（如 Chromaprint）做相似度比对，覆盖"片段侵权"检测
2. **治理升级**：争议增加多组织背书投票仲裁流程；引入 Fabric RAFT 共识替代 Solo Orderer，提升网络可靠性
3. **业务扩展**：增加版税分成结算（结合状态机记录每次使用费用）；支持 NFT 化版权凭证对接外部交易市场
4. **安全加固**：JWT 密钥与数据库凭据接入密钥管理；登记接口增加音频文件类型/大小白名单校验与内容安全检测
5. **性能优化**：引入消息队列削峰登记请求；MySQL 版权缓存表与链上数据定时对账
